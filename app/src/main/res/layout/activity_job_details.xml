<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/login_background"
    android:fitsSystemWindows="true">

    <!-- شريط الأدوات -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbarJobDetails"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:backgroundTint="@color/colorPrimary"
        android:title="Job Details"
        android:titleTextColor="@android:color/white"
        app:titleCentered="true"
        android:elevation="6dp" />

    <!-- المحتوى -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="?attr/actionBarSize"
        android:padding="16dp">

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="8dp"
            android:backgroundTint="@android:color/white"
            app:cardCornerRadius="16dp"
            app:cardElevation="8dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="20dp">

                <!-- عنوان الوظيفة -->
                <TextView
                    android:id="@+id/jobTitle"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Senior Android Developer"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:textColor="#212121"
                    android:paddingBottom="8dp" />

                <!-- اسم الشركة -->
                <TextView
                    android:id="@+id/jobCompany"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Cashly Tech Inc."
                    android:textSize="16sp"
                    android:textColor="#444444"
                    android:paddingBottom="4dp" />

                <!-- الموقع -->
                <TextView
                    android:id="@+id/jobLocation"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Remote - Gaza, Palistaine"
                    android:textSize="14sp"
                    android:textColor="#777777"
                    android:paddingBottom="16dp" />

                <!-- وصف الوظيفة -->
                <TextView
                    android:id="@+id/jobDescription"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="We're looking for a highly skilled Android developer with a passion for mobile technologies to join our team and deliver next-gen financial solutions."
                    android:textSize="15sp"
                    android:textColor="#333333"
                    android:lineSpacingExtra="6dp" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </ScrollView>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
